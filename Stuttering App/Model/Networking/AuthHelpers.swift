//
//  AuthHelpers.swift
//  Stuttering App
//
//  Shared OAuth/nonce utilities — single source of truth for Google Sign-In
//  flows across LoginViewController and SignUpViewController.
//

import Foundation
import CryptoKit
import Security

enum AuthHelpers {

    /// Generates a cryptographically secure random nonce string.
    /// - Parameter length: Desired character length (default 32).
    /// - Returns: A URL-safe random string suitable for OAuth nonces.
    static func randomNonceString(length: Int = 32) -> String {
        precondition(length > 0)
        let charset: [Character] = Array("0123456789ABCDEFGHIJKLMNOPQRSTUVXYZabcdefghijklmnopqrstuvwxyz-._")
        var result = ""
        var remainingLength = length

        while remainingLength > 0 {
            let randoms: [UInt8] = (0..<16).map { _ in
                var random: UInt8 = 0
                let errorCode = SecRandomCopyBytes(kSecRandomDefault, 1, &random)
                if errorCode != errSecSuccess {
                    fatalError("Unable to generate nonce. SecRandomCopyBytes failed with OSStatus \(errorCode)")
                }
                return random
            }
            randoms.forEach { random in
                if remainingLength == 0 { return }
                if random < charset.count {
                    result.append(charset[Int(random)])
                    remainingLength -= 1
                }
            }
        }
        return result
    }

    /// Returns the SHA-256 hex digest of the given string.
    static func sha256(_ input: String) -> String {
        let inputData = Data(input.utf8)
        let hashedData = SHA256.hash(data: inputData)
        return hashedData.compactMap { String(format: "%02x", $0) }.joined()
    }
}
